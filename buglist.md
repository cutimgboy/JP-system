1. "我的页：
（1）顶部资金栏区域距离手机屏幕间距太大，需要缩小间距，且与其他一级页面顶部的间距大小保持一致
（2）尊享特权的广告位，可以手动滑动切换
（3）“APP（IOS&Android）”的文案改为“安装APP”"


2. "存入资金页：
（1）更新设计稿上的最新UI样式
（2）在入金流程里，不展示底部一级页面导航栏，让用户更沉浸式去走入金。"


3. "添加银行卡&上传转账凭证页：
（1）更新设计稿上的最新UI样式
（2）在添加银行卡&上传转账凭证页，不展示底部一级页面导航栏，让用户更沉浸式去走入金。"


4. "“已提交”窗口：
（1）点击“提交转账凭证”之后，会弹出“已提交”弹窗，不要再单独跳转到新的页面了。
（2）“已提交”弹窗按照设计稿去更新最新UI样式"


5. "补充身份信息页：
（1）顶部文案需要更改为“为了保障您的资金安全并符合监管要求，首次大额出金需完成身份信息补充。信息仅用于身份核实。”
（2）上传证件照片，用户上传之后，需要展示预览图
注：后台需要新增一个用户身份信息管理页面，主要储存用户上传的证件信息"


6. "添加银行卡页：
（1）更新设计稿上的最新UI样式
（2）不展示底部一级页面导航栏
（3）用户填写完所有内容后，底部“确认添加”按钮需要显示“可点击”状态"


7. "个人信息页：
（1）编辑个人信息的时候，只可以修改：头像和昵称
（2）注册时间未展示出来，需要展示用户注册的时间
（3）手机绑定和邮箱绑定，无法编辑，但可以点击跳转到后续的更换和新增绑定流程。"


8. "新增手机绑定流程： 
点击“个人信息-手机绑定”会跳转到对应流程
（1）若已经绑定了手机，则跳转更换绑定手机号页面。 更换绑定手机号流程：需获取当前绑定手机号验证码，校验通过后，然后再输入新的手机号和验证码，验证码校验通过后，则更换绑定手机号成功，用户无需重新登录，及换绑成功。 
（2）若无绑定手机号，则跳转到绑定新手机号页面。 新增绑定手机号流程：需获取当前已绑定邮箱账号验证码，校验通过后，然后再输入需要绑定的手机号和验证码，手机验证码校验通过后，则绑定手机号成功。
根据设计稿，补充对应的流程"


9. "新增邮箱绑定： 
点击“个人信息-邮箱绑定”会跳转到对应流程
（1）若已经绑定了邮箱，则跳转更换邮箱绑定页面。 更换绑定邮箱流程：需获取当前绑定邮箱的验证码，，校验通过后，然后再输入新的邮箱地址和验证码，验证码校验通过后，则更换绑定邮箱成功，用户无需重新登录，及换绑成功。 
（2）若无绑定邮箱，则跳转到绑定新邮箱页面。 新增绑定邮箱流程：需获取当前已绑定手机号的验证码，校验通过后，然后再输入需要绑定的邮箱地址和验证码，邮箱验证码校验通过后，则绑定新邮箱成功。"


10. "消息中心页
（1）消息中心页面的UI设计，需要跟换成符合当前APP色调和背景的"
